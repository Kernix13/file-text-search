// import { useState, useEffect } from "react";

const SearchForm = () => {
  return ( 
    <>
    {/* <form> element of component here */}
      <p>A form will go above later with an input of type 'search' and a submit button. Once I have the React UI working with the API project, just like the Console project, I will use the Console project to instead just output result summaries, warnings, errors, etc.</p>
      <p>The search results will display as cards in a CSS Grid.</p>
      <p><strong>I need to add some styling here!!!</strong></p>
    </>
   );
}
 
export default SearchForm;